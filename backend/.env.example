# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=Cluster0

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:4201

# Email Configuration
# Set EMAIL_ENABLED=false to disable email notifications
EMAIL_ENABLED=false

# SMTP Settings (only needed if EMAIL_ENABLED=true)
# For Gmail: Use App Password (not regular password)
# 1. Enable 2FA on your Google Account
# 2. Go to: Google Account > Security > 2-Step Verification > App passwords
# 3. Generate app password and use it below
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-char-app-password

# For other providers:
# SendGrid: EMAIL_HOST=smtp.sendgrid.net, EMAIL_PORT=587, EMAIL_USER=apikey
# Mailgun: EMAIL_HOST=smtp.mailgun.org, EMAIL_PORT=587
# Office365: EMAIL_HOST=smtp.office365.com, EMAIL_PORT=587, EMAIL_SECURE=true

# =============================================================================
# PDF Converter Configuration (PDFtoXMLUsingExcel)
# =============================================================================
# Directory containing the PDF converter (relative to backend folder)
PDF_CONVERTER_DIR=./PDFtoXMLUsingExcel

# Python executable (auto-detects if not set)
# Windows: Auto-detects venv or uses 'python' command
# Linux/Mac: Auto-detects venv or uses 'python3' command
# To override, specify full path or command:
#   Windows example: PDF_CONVERTER_PYTHON=python
#   Linux example: PDF_CONVERTER_PYTHON=python3
#   Custom path: PDF_CONVERTER_PYTHON=C:/Python310/python.exe
# PDF_CONVERTER_PYTHON=python

# Main PDF converter script name (just filename, not full path)
PDF_CONVERTER_SCRIPT=pdf_to_unified_xml.py

# =============================================================================
# EPUB/RittDoc Converter Configuration (RittDocConverter)
# =============================================================================
# Python executable for RittDoc converter (auto-detects if not set)
# Same auto-detection logic as PDF_CONVERTER_PYTHON
# CONVERTER_PYTHON=python

# Full path to the integrated_pipeline.py script (relative to backend folder)
CONVERTER_SCRIPT_PATH=./RittDocConverter/integrated_pipeline.py
